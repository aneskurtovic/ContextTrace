/target
**/*.rs.bk
Cargo.lock.orig

# Local-only real-session corpus used by the smoke test. Real agent sessions
# contain source code, file paths, prompts and potentially secrets -- they are
# NEVER committed. Fixtures are synthetic or carefully redacted captures, as
# recorded in tests/fixtures/compatibility.json.
/corpus/
/tests/corpus/

# Local-only working notes. Keep private implementation plans and backlog
# material under docs/internal/; summarize only durable, public guidance in docs/.
/docs/internal/

# Tauri (milestone 3)
/crates/ct-ui/target
/crates/ct-ui/dist
/crates/ct-ui/src-tauri/gen/
/crates/ct-ui/src-tauri/icons/android/
/crates/ct-ui/src-tauri/icons/ios/
node_modules/
