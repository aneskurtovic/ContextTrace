/target
**/*.rs.bk
Cargo.lock.orig

# Local-only real-session corpus used by the smoke test. Real agent sessions
# contain source code, file paths, prompts and potentially secrets -- they are
# NEVER committed. Committed fixtures are hand-authored synthetic JSONL only.
/corpus/
/tests/corpus/

# Working notes from the Superpowers skills -- design specs and implementation
# plans written to drive a single piece of work. The decisions that outlive
# them belong in commit messages, BACKLOG.md and docs/.
/docs/superpowers/

# Tauri (milestone 3)
/crates/ct-ui/target
/crates/ct-ui/dist
/crates/ct-ui/src-tauri/gen/
/crates/ct-ui/src-tauri/icons/android/
/crates/ct-ui/src-tauri/icons/ios/
node_modules/
