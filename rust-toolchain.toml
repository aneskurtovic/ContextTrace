[toolchain]
# Use the stable toolchain for the host platform. A host-neutral channel keeps
# the repository buildable on Windows, macOS and Linux; rustup selects the
# native target. The previous Windows-GNU pin stopped linking when the local
# LLVM-MinGW installation no longer supplied GCC runtime libraries.
channel = "stable"
components = ["rustfmt", "clippy"]
