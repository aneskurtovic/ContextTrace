[package]
name = "ct-application"
description = "ContextTrace use cases. Orchestrates domain services over ports; depends on no concrete adapter."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ct-domain.workspace = true
serde.workspace = true
serde_json.workspace = true
chrono.workspace = true
thiserror.workspace = true

# A dev-dependency on purpose. This crate derives `Serialize` but names no
# concrete format -- which bytes an export becomes is the CLI's business. The
# tests need a parser to check the records a consumer actually sees, and that
# need must not put a JSON codec into the shipped dependency graph.
[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
